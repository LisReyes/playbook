//example5: Using mao to conver all of the names in a lower Case list
const names = ['Asabeneh', 'Mathias', 'Elias','Brook']
const namesToUpperCase = names.map((name) => name.toUpperCase())

console.log("Example 5: using map to conver all of the names in a lower case list")
console.log(namesToUpperCase)
